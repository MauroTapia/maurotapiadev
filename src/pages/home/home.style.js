import styled from 'styled-components';

export const HomeWrapper = styled.div`
  width: 100%;
  min-height: calc(100vh - 80px); /* Resta el alto del encabezado si tienes uno */
  overflow-x: hidden;
`;